module.exports = {
  // 每个文件格式化的范围
  rangeStart: 0,
  rangeEnd: Infinity,
  // 一行最多多少字符
  printWidth: 80,
  // 使用2个空格缩进
  tabWidth: 2,
  // 使用tab缩进，不使用空格
  useTabs: false,
  // 行尾需要分号
  semi: true,
  // 使用单引号
  singleQuote: false,
  // 对象的key仅在必要时使用引号
  quoteProps: "as-needed",
  // 箭头函数，只有一个参数的时候，也需要括号
  arrowParens: "always",
  // 不需要写文件开头的@prettier
  requirePragma: false,
  // 大括号内的收尾需要空格
  bracketSpacing: true,
  // 不需要自动在文件开头插入@prettier
  insertPragma: false,
  // 使用默认的折行标准
  proseWrap: "always",
  // 根据显示样式决定html要不要折行
  htmlWhitespaceSensitivity: "strict",
  // 换行符使用lf
  endOfLine: "lf",
  // 在对象或数组最后一个元素后面加逗号
  trailingComma: "all",
  // 闭合标签>是否放于行尾
  bracketSameLine: false,
  // vue文件中的script和style标签内不进行最外层缩进
  vueIndentScriptAndStyle: false,
  // template中每个属性单独一行
  singleAttributePerLine: false,
};

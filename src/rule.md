# 校验提示优先级

1. 必填校验（blur 触发、change 内容至空触发）
2. 长度校验（最短长度：只在 blur 时触发，最大长度：会在 change 和 blur 时触发）
3. 格式校验（只在 blur 触发）

# 弹窗宽度

## 普通表单弹窗

1. 800px（双列）
2. 400px（单列，占 1/2 行的元素不大于四个时使用，或者数据少好看就用）

## 复杂弹窗（如有表格的弹窗等）

1. 1200px（大）

# 延时

1. 快速延时：100ms
2. 正常延时：300ms
